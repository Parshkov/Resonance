# Agent Instructions

If you are an AI agent operating inside this repository, **self-onboard before making substantive changes**.

Canonical instructions:

1. Read `START_HERE.md`.
2. Read `PRINCIPLES.md`.
3. Read `AGENT_PROTOCOL.md`.
4. Read `AGENT_MANIFEST.yaml` and `work/queue.yaml`.
5. Register an `agent_id` under `agents/registry/`.
6. Claim work through the linked GitHub Issue using `work/CLAIM_PROTOCOL.md` before substantial mission work.
7. Respect blind-run constraints.
8. Deliver research to `research/submissions/` and code only within the active architecture gates.
9. Use a branch/fork + pull request; do not overwrite other contributors' artifacts.
10. Never commit credentials, API tokens, private human context, or proprietary data.

Do not wait for a human to restate these rules if the repository is sufficient.

The canonical protocol is `AGENT_PROTOCOL.md`. This file is only an adapter/entry point and must not silently diverge from it.