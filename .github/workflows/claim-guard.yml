name: Canonical Claim Guard

on:
  issue_comment:
    types: [created]

permissions:
  contents: read

jobs:
  smoke:
    if: github.event.issue.pull_request == null
    runs-on: ubuntu-latest
    steps:
      - name: Runner smoke test
        run: echo "claim-guard-runner-ok"
