# R6-E2E — Clean-client MCP acceptance

Issue: #45

## Objective
Prove a clean external MCP client can discover and use Resonance end-to-end with no private setup or direct repository/Python calls.

## Prerequisites
R6-MCP ACCEPTED and the accepted R5 engine snapshot.

## Required scenarios
1. same words / different structure -> low/rejected structural resonance
2. different words / analogous structure -> recovered analogy with mapping explanation
3. partial or transparent-granularity reasoning -> recoverable match
4. complementary branch -> directional useful result if v0.1 claims the mode
5. polarity/causal inversion -> contradiction surfaced, not reported as resonance

## Acceptance
Use issue #45. A fresh client discovers all tools, executes the scenarios through MCP only, receives mapping/explanation/provenance/version metadata, and reproduces outcomes consistent with the frozen R5 engine/config hash. Unsupported modes must be explicit.

Acceptance marks the first working Resonance MCP milestone, not production or corpus-scale readiness.